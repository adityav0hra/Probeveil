import { db } from "@/lib/db";
export default async function AuditPage() { const logs = await db.auditLog.findMany({ orderBy: { createdAt: "desc" }, take: 200, include: { user: true } }); return <><p className="eyebrow">Governance</p><h1 className="mt-2 text-3xl font-semibold">Audit logs</h1><p className="mt-2 muted">Immutable record of control-plane actions.</p><section className="panel mt-8 overflow-hidden"><table className="w-full text-left text-sm"><thead className="bg-white/[.02] text-[10px] uppercase tracking-wider text-slate-600"><tr><th className="px-5 py-3">Time</th><th>Actor</th><th>Action</th><th>Resource</th></tr></thead><tbody>{logs.map((log) => <tr key={log.id} className="border-t border-line"><td className="px-5 py-4 text-xs text-slate-500">{log.createdAt.toLocaleString()}</td><td>{log.user?.email ?? "System"}</td><td className="text-slate-300">{log.action.replaceAll("_", " ")}</td><td className="text-xs text-slate-500">{log.resourceType} {log.resourceId?.slice(0, 8)}</td></tr>)}</tbody></table>{logs.length === 0 && <div className="p-12 text-center muted">No audit events yet.</div>}</section></>; }
